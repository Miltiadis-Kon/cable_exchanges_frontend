# ── Kafka Configuration ──────────────────────────────────────
# Bootstrap server address (Aiven)
KAFKA_BOOTSTRAP_SERVERS=imbalance-greece-imbalancegrtable13.b.aivencloud.com:18883

# Topics to consume (comma-separated)
KAFKA_TOPICS=cables,exchanges

# Consumer group ID
KAFKA_GROUP_ID=power-market-preview

# ── SSL Certificates ────────────────────────────────────────
# Option 1: File paths (for local development)
# KAFKA_CERTS_DIR=../certs
# KAFKA_CA_PATH=../certs/ca.pem
# KAFKA_CERT_PATH=../certs/service.cert
# KAFKA_KEY_PATH=../certs/service.key

# Option 2: Base64-encoded certificates (for Render deployment)
# Generate with: cat ca.pem | base64 -w 0
# KAFKA_CA_B64=
# KAFKA_CERT_B64=
# KAFKA_KEY_B64=

# ── Server ───────────────────────────────────────────────────
PORT=3001
